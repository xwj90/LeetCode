using System;
using System.Collections.Generic;
using System.Linq;
public partial class Solution
{
    public ListNode InsertionSortList(ListNode head)
    {
        throw new NotImplementedException("");
        //var fakeHead = new ListNode(int.MinValue);
        //fakeHead.next = head;
        //var current = head; 
        //while (current != null)
        //{
        //    var next = current.next;
        //    //compare
        //    var c = fakeHead;
        //    while (c != current && c.next.val<current.val)
        //    {
        //        c = c.next;        
        //    }
        //    if (c.next != current)
        //    {
        //        var temp = c.next;
        //        c.next = current;
        //        current.next = temp;
        //    }
        //    current = next;
        //}
        //return fakeHead.next;
    }
}