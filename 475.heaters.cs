/*
 * @lc app=leetcode id=475 lang=csharp
 *
 * [475] Heaters
 */
public class Solution
{
    public int FindRadius(int[] houses, int[] heaters)
    {
        // var ht = heaters.OrderBy(p => p).ToList();
        // var result = 0;
        // foreach (var house in houses)
        // {
        //     var i = ht.BinarySearch(house);
        //     if (i < 0)
        //     {
        //         i = -(i + 1);
        //     }
        //     int dist1 = i - 1 >= 0 ? house - heaters[i - 1] : int.MaxValue;
        //     int dist2 = i < heaters.Length ? heaters[i] - house : int.MaxValue;

        //     result = Math.Max(result, Math.Min(dist1, dist2));
        // }

        // return result;
    }
}

