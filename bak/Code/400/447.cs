using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

public partial class Solution
{

    //public int NumberOfBoomerangs(int[,] points)
    //{
    //    double[,] dis = new double[points.GetLength(0), points.GetLength(0)];
    //    for (int i = 0; i < points.GetLength(0); i++)
    //        for (int j = i + 1; j < points.GetLength(0); j++)
    //            dis[i, j] = Math.Pow(points[i, 0] - points[j, 0], 2) + Math.Pow(points[i, 1] - points[j, 1], 2);


    //}
}